import { css } from 'styled-components';
import { extRem } from '../../../src/utils';

export const base = css`
`;

export const preview = css`
  margin-top: ${extRem(30)};
`;

export const code = css`
  margin: ${extRem(40)} auto 0;
  max-width: ${extRem(800)}
`;

